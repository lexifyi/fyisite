import type { ComponentChildren } from "preact";

export function LandingPage(): ComponentChildren {
  return (
    <code style={{ fontSize: "24px" }}>
      i'm lexi and fyi this is my website
    </code>
  );
}
